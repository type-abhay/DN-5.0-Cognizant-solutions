package com.example;

import org.junit.Test;
import static org.junit.Assert.assertTrue;

public class SetupTest {
    @Test
    public void testJUnitSetup() {
        // Simple test to check if JUnit runs correctly
        assertTrue(true);
    }
}